name: GitHub Actions Demo with C
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on:
  push:
    branches: [ $default-branch ]
  pull_request:
    branches: [ $default-branch ]
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: compile
      run: gcc test.c -o test
    - name: runtest
      run: ./test
